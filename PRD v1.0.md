# 产品需求规格说明书（PRD）
版本：1.0
## 1. 项目背景与目标
### 1.1 一句话目标
    构建一个基于 Kubernetes 的多集群容器管理平台，支持公司/部门/成员三级组织权限、多环境（生产/测试/开发）隔离访问，提供服务查看、灰度/批次发布、扩缩容、CI/CD 流水线审批等核心运维能力。

## 1.2 用户场景
    某公司拥有数百个基于 Docker 的微服务，运行在开发、测试、生产三个独立 Kubernetes 集群上（集群间网络不互通）。运维人员、开发人员、部门主管需要根据权限查看或操作对应环境中的服务。
    运维工程师需要跨集群查看服务状态、执行灰度发布、手动扩缩容。
    开发工程师需要在开发/测试环境查看日志、调整实例数。
    部门经理需要审批生产环境的发布任务。
    系统需能根据内存等指标自动扩缩容，并利用 Nacos 服务发现和 Nginx 流量数据辅助决策。

# 2. 用户角色与权限矩阵
## 2.1 角色定义
    角色	描述	典型职责
    系统管理员	全局最高权限，管理公司、部门、成员、集群及环境映射	创建公司/部门、添加成员、配置集群连接、分配角色
    部门主管	负责本部门所有环境（含生产）的审批和操作监管	审批生产环境的发布、扩缩容操作；查看部门所有服务
    运维工程师	日常操作执行者	跨集群查看服务、执行发布/扩缩容、配置自动扩缩策略
    开发工程师	开发/测试环境操作者	查看服务列表、日志、启动命令；在开发/测试环境执行发布、扩缩容
    审计员	只读所有操作日志和配置（只读）	审计发布记录、扩缩容历史、登录行为
## 2.2 权限矩阵
    说明：
    访问控制分为“环境级”（开发、测试、生产）和“公司/部门/成员级”。

    每个成员属于唯一公司 → 部门（可多部门？简化模型：一人可属于多部门，取权限并集，但审批需明确部门归属。为清晰，设计为“主部门+兼任部门”，操作时按操作环境所属部门校验。本 PRD 采用：用户可有多个部门角色，但审批流程中只以任务创建者所在部门为准。）

    三级组织：公司（根节点）→ 部门（公司内独立单元）→ 成员。

    每个服务、每个环境都归属到具体部门。

    权限表（操作类型 × 角色 × 环境）

        操作类型	系统管理员	部门主管	运维工程师	开发工程师	审计员
        用户/组织管理（增删公司/部门/成员，分配角色）	全部公司	仅本部门成员管理（不可删改上级公司）	无	无	无
        集群连接配置（注册K8s集群、测试连通性）	全部集群	无	无	无	只读
        服务查看（列表、状态、日志、资源、启动命令）	所有环境	本部门所有环境	所有环境	仅开发、测试环境	所有环境（只读）
        历史报表查看（服务操作记录、资源使用趋势）	所有环境	本部门所有环境	所有环境	仅开发、测试环境	所有环境（只读）
        手动扩缩容（页面调整实例数）	所有环境（需符合环境规则）	本部门生产环境（需审批？本平台设计为部门主管可直接操作生产，但可要求二次确认；为安全，设“直接操作+事后审计”，不强制审批）	所有环境（但生产环境需部门主管授权临时 token？为了减少流程开销，设计：生产环境的手动扩缩容默认需要部门主管审批，除非在部门设置中开启“运维免批”开关）	仅开发、测试环境	无
        动态扩缩容策略配置（指标、阈值）	所有环境	本部门生产环境（需审批）	所有环境（生产需审批）	仅开发、测试环境	只读
        灰度发布（创建/执行/全量/回滚）	所有环境	本部门生产环境（审批关键阶段）	所有环境（生产环境阶段审批由部门主管执行）	仅开发、测试环境	只读
        批次发布（分批更新）	所有环境	本部门生产环境（审批每批？设计：仅首标审批，后续自动）	所有环境（生产环境需要部门主管审批启动）	仅开发、测试环境	只读
        CI/CD 流水线触发/审批	所有环境（可审批任何流水线）	本部门生产环境的流水线最终审批	创建/执行流水线（生产环境需部门主管审批）	创建/执行开发测试环境流水线	只读
        操作审计日志查看	所有	本部门	所有（只读）	仅本人操作记录	所有
# 3. 多集群管理方案
## 3.1 管理系统自身部署位置
    管理服务（控制台 + API） 部署在 独立的管理网络区 中，该网络能够按需主动连接各个环境集群的 API Server。
    各环境集群（开发、测试、生产）之间互不相通，但管理服务所在网络与每个集群的 API Server 网络可达（通过 VPN 或专线，最小权限原则仅开放 HTTPS 6443 端口）。
    管理服务不接入用户业务流量，只通过 K8s API 操作集群。

## 3.2 连接不同环境集群方式
    每个集群配置一个 Kubeconfig 文件，存储在管理服务的安全凭据存储中（加密）。
    管理服务维护集群注册表，包含：集群名称（唯一）、环境类型（开发/测试/生产）、API Endpoint、证书、认证方式（Token 或客户端证书）。
    当用户执行跨集群操作时，管理服务根据服务所属的环境字段，动态加载对应集群的客户端，并向该 K8s API 发送请求。
    所有跨集群操作记录带有“操作源 IP（管理服务 IP）+ 用户 ID + 目标集群”。

## 3.3 环境隔离策略
    一个服务（逻辑概念）在每个环境中可以存在独立的 Deployment 或 StatefulSet，平台通过“服务实例”概念管理：
    服务标识（app-name） + 环境（env） → 对应一个 K8s 工作负载。
    用户登录后，仅能看见有权限的环境及其服务。
    生产环境的所有变更操作（除了查看和审批）都强制记录操作原因字段。
    开发、测试环境允许更宽松的错误恢复策略（如直接回滚）。

# 4. 功能需求
## 4.1 用户与组织管理
 
### 功能 4.1.1：公司管理
    作为 系统管理员，我想要 创建、编辑、删除公司，以便 隔离不同独立业务实体。
    验收标准：
        删除公司时，若公司下存在部门或成员或服务，则提示并阻止删除。

### 功能 4.1.2：部门管理
    作为 系统管理员/部门主管，我想要 在公司下创建部门，并将成员分配到部门，以便 控制服务归属和审批链路。
    验收标准：
        部门主管只能管理本部门成员（增删改，但不能改角色为系统管理员）。
        每个服务必须关联唯一部门。

### 功能 4.1.3：角色分配
    作为 系统管理员，我想要 对用户授予角色（可多角色），并设置环境访问范围，以便 细化权限。
    验收标准：
        角色可绑定到“所有环境”或“特定环境（开发/测试/生产）”。
        同一用户在不同环境可以拥有不同角色（例如开发环境为开发工程师，生产环境为只读审计员）。

## 4.2 服务查看与监控
### 功能 4.2.1：服务列表
    作为 任意授权角色，我想要 按公司/部门/环境筛选服务列表，并看到每个服务的健康状态、副本数、运行时间，以便 快速掌握全局。
    验收标准：
        列表支持分页，搜索服务名。
        状态从 K8s 实时获取（缓存≤5秒）。

### 功能 4.2.2：服务详情
    作为 任意授权角色，我想要 查看单个服务的详细资源（CPU/内存请求与限制、实际使用趋势）、启动命令（容器 args）、环境变量，以便 排查问题。
    验收标准：
        启动命令从 Pod Spec 中解析。
        资源使用图表展示最近 1 小时/6 小时/24 小时。

### 功能 4.2.3：实时日志
    作为 运维/开发工程师，我想要 通过 WebSocket 获取 Pod 实时日志（支持级别过滤、关键词高亮），以便 实时调试。

    验收标准：
        可选择 Pod 实例及容器名。
        日志保留最近 1 万行，支持暂停/滚动。

### 功能 4.2.4：历史数据报表
    作为 部门主管/运维/审计员，我想要 查看某服务一定时间范围内的操作记录（发布、扩缩容、配置变更）、资源使用峰值、实例重启次数，以便 生成运维报表。

    验收标准：
        报表可按日/周/月聚合导出 CSV。
        数据来源：平台操作日志 + 监控系统历史指标。

## 4.3 灰度发布
    定义：灰度发布允许新版本先引入内部测试（不接入外部真实流量），验证符合预期后逐步接入真实流量至全量。

### 功能 4.3.1：创建灰度任务
    作为 运维工程师，我想要 为某个生产服务创建一个灰度发布任务，指定新镜像版本、灰度策略（内部测试→5%→25%→100%），以便 控制上线风险。
    验收标准：
        任务需关联部门主管审批节点：内部测试阶段完成后，请求接入真实流量时需要审批（可配置是否自动）。
        灰度期间，平台自动创建两个版本集（稳定版和金丝雀版），通过 Nginx 流量权重切换。

### 功能 4.3.2：灰度流程阶段（明确阶段和审批节点）
    阶段	动作描述	流量接入情况	是否需要审批	审批角色
    0. 任务创建	提交灰度任务，指定新镜像、内部测试实例数（默认1个 Pod，不接流量）	无真实流量，仅内部测试 endpoint（平台内部标记）	否，但部门主管可收到通知	-
    1. 内部测试	平台在集群中启动金丝雀 Pod，不注册到 Nacos 或注册但权重0；测试人员通过平台提供的“内部访问链接”验证功能	无真实用户流量	否（测试人员自助）	-
    2. 请求接入流量	测试人员点击“准备接流”，系统等待审批	仍然无流量	是，部门主管审批	部门主管
    3. 小流量验证	审批通过后，平台配置 Nginx 将 5% 流量分配给金丝雀版本，同时监控关键指标（错误率、延迟）	5% 真实流量	否（自动进入，但允许人工暂停）	-
    4. 扩大流量（25%）	观察期（默认 30 分钟）无异常，可手动点击“扩大至25%”或配置自动扩大	25%	仅当失败率超过阈值（5%）时暂停并告警	-
    5. 全量	流量逐步扩大至 100%，然后将旧版本缩至 0（或保留备份）	100%	无额外审批（但部门主管可一键回滚）	-
    6. 完成	删除旧版本 Deployment，保留新版本作为稳定版	100%	-	-

    验收标准：
        每个阶段均可回滚，回滚后流量 100% 切回旧版本，且无需审批（但记录操作）。
        灰度过程中，如果金丝雀版本 Pod 健康检查失败，自动暂停扩大流量并告警。

## 4.4 批次发布
    定义：针对单个服务的大量实例（例如 200 个副本），分批更新，每批更新一部分实例，观察后再继续。

### 功能 4.4.1：创建批次发布任务
    作为 运维工程师，我想要 选择服务（环境不限，但生产需审批），设置批次策略（每批实例数/百分比、批次间隔时间、失败回滚条件），以便 平滑更新。
    验收标准：
        对于 StatefulSet，按序号顺序分批；对于 Deployment，使用 maxSurge 配合分区控制（或基于 K8s 滚动更新的 pause 特性）。

### 功能 4.4.2：批次策略详细规格
每批实例数：固定数量（如 10 个）或百分比（如 20%）。
观察间隔：每批更新完成后，等待 N 秒（默认 60 秒）或等待人工确认。
回滚条件：本批更新后，若该批 Pod 的“就绪比例”< 80% 或错误日志突增（可关联指标），自动回滚全部批次。
最大批次：最多分 10 批，若实例数超过单批上限，系统自动切分。

验收标准：
    支持“无人值守模式”（按间隔自动下一批）和“手动确认模式”（每批需点击确认）。
    任意批次失败时，可停止后续批次并一键回滚到初始版本。

## 4.5 扩缩容
### 4.5.1 手动扩缩容
    功能：作为 授权角色，在服务详情页可直接修改实例数（副本数）。
    验收标准：
        操作时需填写原因（生产环境必填）。
        若用户无生产环境审批权，则操作会生成审批单，部门主管审批后执行。

### 4.5.2 动态扩缩容（基于指标）
    功能：支持配置水平自动扩缩策略（HPA 风格），但需统一指标来源和决策逻辑。

    指标来源：
    主要来源：Prometheus 监控系统（已采集各容器 CPU、内存、网络流量，以及 Nginx 入口流量和 Nacos 服务请求速率）。
    若监控系统不可用，平台自建轻量级采集器 fallback（通过 K8s metrics-server 获取基础指标）。

    决策逻辑：
    管理员为每个服务（或部门级默认策略）配置扩缩策略：
    目标指标类型：内存使用率（百分比）、CPU 使用率、或自定义（如每秒请求数 QPS）。

    期望阈值：例如内存 > 75% 扩容，< 30% 缩容。

    步长：每次调整的实例数（最小 1，最大不超过当前副本数的 50%）。

    冷却时间：两次扩缩操作之间的最小间隔（默认 5 分钟）。

    平台周期性（30 秒）从 Prometheus 拉取指标。
    若指标持续超过阈值（连续 3 个采样点），计算期望副本数：
    期望副本数 = ceil(当前副本数 * (当前指标值 / 期望阈值))，并限制最大副本数（用户配置上限默认 100）。
    调用 K8s API 调整 Deployment/StatefulSet 副本数。
    缩容时需判断“低负载持续 10 分钟”才执行，避免抖动。

    验收标准：
        支持针对不同环境启用/禁用动态扩缩。
        所有自动扩缩操作有审计日志，并可在平台上看到“扩缩事件”列表。

## 4.6 CI/CD 流水线与审批
### 功能 4.6.1：流水线定义
    作为 运维/开发工程师，我想要 在平台上创建流水线（关联代码仓库、构建镜像、推送到仓库、触发部署任务），以便 自动化发布。
    验收标准：
        流水线步骤支持：代码拉取、镜像构建、单元测试、镜像扫描、部署到目标环境（调用灰度/批次发布接口）。

### 功能 4.6.2：流水线审批
    部署到生产环境的流水线阶段，必须经过部门主管审批。
    审批节点可设置在“部署前”。
    审批超时（24 小时）默认拒绝，并通知创建人。
    验收标准：
        审批单包含本次变更的镜像版本、环境、变更说明。
        审批人可批复“通过/拒绝/转交”。

## 4.7 与外部系统集成点
外部系统	            集成点	        数据流向	说明
K8s 集群（多个）	    API Server	    双向	    管理服务通过 client-go 操作资源（Deployment, Service, Pod 等）
Nacos	                服务发现注册	读取	    获取服务实例健康状态、流量权重（用于灰度权重验证）
Nginx                   流量管理	    写入	    动态修改 Ingress 的 canary 权重（灰度）
Prometheus	            指标查询	    只读	    拉取容器资源使用率、Nginx 请求速率，用于扩缩容及展示
ELK日志系统             日志查询	    只读	    平台可跳转或内嵌日志视图（备选，实时日志优先从 K8s API 获取）
# 5. 非功能需求
## 5.1 性能
    - 服务列表加载时间 ≤ 2 秒（100 服务 + 5000 Pods 场景）。
    - 实时日志延迟 ≤ 1 秒（从产生到显示）。
    - 扩缩容 API 响应时间 ≤ 1 秒（不含 K8s 实际操作）。
    - 同时操作并发 ≤ 10 时，系统 CPU 利用率 ≤ 50%。

## 5.2 安全
    - 所有用户操作需认证（JWT token，有效期 8 小时）。
    - 敏感操作（发布、扩缩容、删除）二次验证（短信或 TOTP 可选）。
    - 操作日志记录不可篡改（存储到单独的审计数据库，只追加）。
    - 各集群 Kubeconfig 存储加密（AES-256）。

## 5.3 可用性
    - 管理服务自身支持高可用（至少 2 个实例 + 负载均衡）。
    - 管理服务与某个集群网络断开时，不影响其他集群操作，并告警。
    - 关键数据（用户、权限、任务）每日备份。

## 5.4 可扩展性
    - 支持未来添加新集群（只需配置 Kubeconfig）。
    - 指标采集可插拔（Prometheus / 其他）。

# 6. 数据模型概念（逻辑实体）
    实体	            主要属性
    Company	            ID, 名称, 创建时间
    Department	        ID, 所属公司 ID, 名称, 主管用户 ID
    User	            ID, 姓名, 邮箱, 所属部门列表
    Role	            ID, 名称（系统管理员/部门主管/运维/开发/审计员）, 环境权限掩码
    Cluster	            ID, 名称, 环境类型, API Endpoint, 认证凭据引用
    ServiceInstance	    ID, 服务名, 所属部门 ID, 所属集群 ID, K8s 工作负载类型（Deployment/StatefulSet）, namespace
    DeploymentTask	    ID, 类型（灰度/批次）, 服务实例ID, 状态, 阶段信息, 审批记录
    AutoScalingPolicy	ID, 服务实例ID, 指标类型, 阈值, 最大/最小副本数, 冷却秒数
    Pipeline	        ID, 名称, 步骤JSON, 触发方式, 最近运行状态

# 7. 风险提示
    - 跨集群网络风险：管理服务与各集群 API Server 的网络必须稳定，否则会出现“部分集群不可操作”。建议每个集群配置备份 API Server 地址。
    - 灰度流量切换依赖 Nginx：要求 Nginx Ingress Controller 支持 canary 权重（如 nginx.ingress.kubernetes.io/canary-weight）。若现有 Nginx 版本不支，需要升级或改用其他方案（如 Istio）。
    - 动态扩缩指标来源一致性：Prometheus 采集可能存在延迟，建议在文档中明确指标最大滞后时间（如 30 秒），并允许用户配置防抖采样。
    - 批次发布的分批策略：对于大规模 StatefulSet，K8s 原生滚动更新按序号逆序，平台批次控制可能需要借助 partition 参数实现，确保技术选型时确认可行性。